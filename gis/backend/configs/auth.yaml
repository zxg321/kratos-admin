authn:
    jwt:
        secret: dev-gis-jwt-secret-change-me
        method: HS256
        access_token_expires: 3600s
        refresh_token_expires: 2592000s
        white_list:
            # M1 冒烟期放行 GIS 全部接口，接入完整权限体系后移除。
            prefix:
                - /gis.admin.v1.
            match: []
            path: null
            regex: null
