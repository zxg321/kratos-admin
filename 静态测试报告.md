# 静态测试报告

- 测试日期：2026-09-13
- 范围：`backend`（Go + Kratos）、`frontend/admin`（管理后台）、`frontend/uni-app`（应用端）
- 结论：**共发现 10 处问题，全部为后端同一类并发安全 Bug（struct 按值复制锁）**；两个前端模块全部通过，未发现任何问题。

| 模块 | 检查工具 | 结果 |
| --- | --- | --- |
| backend | `go build ./...` | ✅ 通过 |
| backend | `go vet ./...` | ❌ 发现 10 处 Bug（详见下文） |
| backend | `golangci-lint run ./...` | ✅ 通过（0 issues） |
| frontend/admin | `oxlint`（441 个文件） | ✅ 通过（0 错误） |
| frontend/admin | `prettier` / `stylelint` | ✅ 通过 |
| frontend/admin | `type:check`（vue-tsc） | ✅ 通过（5/5） |
| frontend/uni-app | `eslint .` | ✅ 通过（0 错误） |
| frontend/uni-app | `tsc --noEmit` | ✅ 通过（3/3） |

---

## 一、后端 Bug（共 10 处）

### 问题概述

`go vet` 的 **copylocks** 检查发现 10 处按值复制 `BaseLogFallbackConfig` 的代码。该类型是 protobuf 生成的消息类型（type alias，见 `internal/biz/base/runtimeconfig/runtimeconfig.go:23`），其内嵌的 `protoimpl.MessageState` 包含 `sync.Mutex`。按值复制会连带复制互斥锁，导致：

1. **锁状态被复制**：复制品的锁状态与原锁一致，若原锁正被持有，复制品将永久死锁；
2. **失去互斥保护**：对复制品的加锁/解锁操作与原对象完全隔离，并发访问内嵌字段时无同步保护。

**报错签名**（10 处完全相同）：

```
... copies lock value: github.com/liujitcn/kratos-admin/backend/internal/biz/base/runtimeconfig.BaseLogFallbackConfig
contains google.golang.org/protobuf/runtime/protoimpl.MessageState contains sync.Mutex
```

**修复方向**（三选一，按推荐度排序）：

1. 函数间改传指针 `*BaseLogFallbackConfig`（改动最小，见下方逐条建议）；
2. 需要副本时改用 `proto.Clone(config)` 深拷贝（返回值类型同步改为指针）；
3. 不再缓存整个 protobuf 消息，改为缓存一个只含业务字段的普通 struct（如 `file_path`），从根源消除锁复制。

### 生产代码（7 处）

#### BUG-01：`loadBaseLogFallbackConfig` 返回值按值复制锁

- 文件：`backend/internal/server/middleware/log/log.go:886`
- 报错（含全部 6 个 vet 报错点）：

```
log.go:888:12: assignment copies lock value to config
log.go:894:10: return copies lock value
log.go:896:12: assignment copies lock value to latest
log.go:900:10: return copies lock value
log.go:903:21: assignment copies lock value to m.fallbackConfig
log.go:905:9: return copies lock value
```

- 问题代码：

```go
// loadBaseLogFallbackConfig 读取最新回退配置，缓存不可用时使用最近一次有效配置。
func (m *Middleware) loadBaseLogFallbackConfig() runtimeconfig.BaseLogFallbackConfig {
	m.fallbackConfigMu.RLock()
	config := m.fallbackConfig        // ← 888: 按值赋值，复制锁
	m.fallbackConfigMu.RUnlock()
	if config.FilePath == "" {
		config = runtimeconfig.DefaultBaseLogFallbackConfig()
	}
	if m.configCache == nil {
		return config            // ← 894: 按值返回，复制锁
	}
	latest := config            // ← 896: 按值赋值，复制锁
	err := runtimeconfig.LoadJSON(m.configCache, runtimeconfig.BaseLogFallbackKey, &latest)
	if err != nil {
		log.Warn("读取日志入库回退配置失败，使用最近一次有效配置", "error", err)
		return config        // ← 900: 按值返回，复制锁
	}
	m.fallbackConfigMu.Lock()
	m.fallbackConfig = latest      // ← 903: 按值赋值，复制锁
	m.fallbackConfigMu.Unlock()
	return latest              // ← 905: 按值返回，复制锁
```

- 说明：此函数虽然用 `fallbackConfigMu` 保护了对 `m.fallbackConfig` 的读写，但每条赋值/返回语句都会把消息值（连同内嵌锁）整体复制一份，锁的运行期状态随值一起被拷走，保护形同虚设。此外 `runtimeconfig.LoadJSON(..., &latest)` 以指针方式注入缓存值，与外部按值缓存操作并发进行时存在数据竞争风险。
- 建议：缓存与传参统一改为指针（`*BaseLogFallbackConfig`），需要副本时使用 `proto.Clone`；默认值场景返回 `proto.Clone(Default...)`。

#### BUG-02：`replayBaseLogFallback` 按值接收 config，`Exec` 按值传参

- 文件：`backend/internal/task/system/admin/base_log_fallback.go:73` 与 `:100`
- 报错：

```
base_log_fallback.go:73:47: call of t.replayBaseLogFallback copies lock value
base_log_fallback.go:100:81: replayBaseLogFallback passes lock by value
```

- 问题代码：

```go
// base_log_fallback.go:66
func (t *BaseLogFallbackTask) Exec(ctx context.Context, _ map[string]string) ([]string, error) {
	config := runtimeconfig.DefaultBaseLogFallbackConfig()
	err := runtimeconfig.LoadJSON(t.configCache, runtimeconfig.BaseLogFallbackKey, &config)
	...
	replayed, err = t.replayBaseLogFallback(ctx, config)   // ← 73: 按值传参，复制锁
	...
}

// base_log_fallback.go:100
func (t *BaseLogFallbackTask) replayBaseLogFallback(ctx context.Context, config runtimeconfig.BaseLogFallbackConfig) (int, error) {
	// ← 100: 签名按值接收，整个调用链复制锁
```

- 建议：`replayBaseLogFallback` 的 `config` 参数改为 `*runtimeconfig.BaseLogFallbackConfig`，调用处传 `&config`。

### 测试代码（3 处）

#### BUG-03：`mustJSON` 测试辅助函数按值接收 any 并 json.Marshal

- 文件：`backend/internal/biz/base/runtimeconfig/runtimeconfig_test.go:12`
- 报错：

```
runtimeconfig_test.go:12:57: call of mustJSON copies lock value
```

- 问题代码：

```go
func TestValidateProtoRuntimeConfig(t *testing.T) {
	valid := DefaultBaseLogFallbackConfig()
	if err := ValidateJSON(BaseLogFallbackKey, mustJSON(t, valid)); err != nil {   // ← 12: 按值传 valid
		...
	}
}
```

- 建议：`mustJSON(t *testing.T, value any)` 内部已用 `json.Marshal(value)`（接口持有原值），改为传指针 `mustJSON(t, &valid)` 即可，`mustJSON` 签名无需变动。

#### BUG-04：`json.Marshal` 直接序列化按值传入的消息

- 文件：`backend/internal/server/middleware/log/log_test.go:370`
- 报错：

```
log_test.go:370:29: call of json.Marshal copies lock value
```

- 问题代码：

```go
func newLogStorageTestCache(t *testing.T, directory string) cache.Cache {
	...
	config := runtimeconfig.DefaultBaseLogFallbackConfig()
	config.FilePath = directory
	value, err := json.Marshal(config)   // ← 370: 按值序列化，复制锁
	...
}
```

- 说明：`json.Marshal` 通过反射按值访问结构体字段，同样触发锁复制；标准做法是传 `&config`（`json.Marshal` 对 proto 消息按指针序列化也更安全）。
- 建议：改为 `json.Marshal(&config)`。

> 注：BUG-03 / BUG-04 位于 `_test.go` 文件，不影响生产运行，但 `go vet ./...` 在 CI 中会持续失败；修复方式与生产代码一致（传指针）。

### 修复验证

修复后重跑以下命令验证（注意先执行 `env -u GOROOT`，见附录环境说明）：

```bash
cd backend
env -u GOROOT /opt/homebrew/bin/go vet ./...
env -u GOROOT /opt/homebrew/bin/golangci-lint run ./...
```

两条命令均无输出即通过。

---

## 二、frontend/admin（管理后台）

| 检查项 | 工具 / 命令 | 结果 |
| --- | --- | --- |
| JS/TS/Vue 静态检查 | `oxlint`（含 vue 插件，自定义配置 `internal/lint-config/oxlint.json`） | ✅ 441 个文件，0 错误 |
| 格式化检查 | `prettier --check` | ✅ 通过 |
| CSS 检查 | `stylelint` | ✅ 通过 |
| 类型检查 | `type:check`（vue-tsc） | ✅ 5/5 通过 |

未发现问题。

## 三、frontend/uni-app（应用端）

| 检查项 | 工具 / 命令 | 结果 |
| --- | --- | --- |
| JS/TS/Vue 静态检查 | `eslint .`（eslint 9） | ✅ 0 错误 |
| 类型检查 | `tsc --noEmit` | ✅ 3/3 通过 |

未发现问题。

---

## 附录：环境问题说明（非代码 Bug）

本次检查过程中踩到的环境坑，已绕过但未修改任何文件，记录如下：

1. **GOROOT 环境变量覆盖导致工具链不匹配**：本机 shell 中固定了 `GOROOT=/Users/liu/Documents/go/env/go1.25.12`，而 `backend/go.mod` 声明 `go 1.27.0`，且依赖 `github.com/liujitcn/kratos-kit/auth@v0.0.28` 使用了 Go 1.26+ 才进入标准库的 `uuid` 包。结果：
   - 默认 `go vet ./...` 直接报 `package uuid is not in std`；
   - 即使改用 brew 安装的 go 1.27.1（`/opt/homebrew/bin/go`），仍因 GOROOT 环境变量被拉回 1.25.12 标准库而失败；
   - golangci-lint 首跑也因此报出 2 个 typecheck 环境错误（`internal/i18n/i18n.go:4` 与 `internal/openapi/openapi.go:4` 的 embed import 报 `compile: version go1.25.12 does not match go tool version go1.27.1`）。
2. **解决办法**：统一使用 `env -u GOROOT /opt/homebrew/bin/go ...` 与 `env -u GOROOT /opt/homebrew/bin/golangci-lint run ./...` 执行后端检查。修复环境后 golangci-lint 输出 `0 issues`，之前的 2 个 typecheck 错误消失（确认是环境问题而非代码问题）。
3. **前端 lint 脚本带 `--fix` 会改文件**：为保持本次检查只读，未使用 package.json 中的 `lint` 脚本，改为去掉 `--fix` 直接调用底层工具（`pnpm exec oxlint ...`、`pnpm exec eslint .`）。
