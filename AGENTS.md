# Codex 规则（仓库根）

## 仓库结构
- `backend`：Go + Kratos 后端；后端业务、接口契约、数据库访问、服务启动改动统一在此完成。
- `frontend/admin`：管理后台；页面、组件、样式、构建配置改动统一在此完成。
- `frontend/uni-app`：uni-app 应用端；页面、分包、应用端接口调用与多端兼容改动统一在此完成。
- `backend/migration`：数据库初始化迁移与初始化数据；当前统一维护在 `v0.0.1`。
- 模块业务代码不要散落到其他模块目录，避免跨目录职责漂移。

## 重构原则
- 每次重构默认不考虑向后兼容；只有需求明确要求时才保留兼容层或兼容行为。

## 数据库注释
- 所有数据库表和字段必须使用中文注释。admin 库建表/改表后需在开发库补齐中文 `COMMENT` 再执行 `make gorm-gen` 同步；GIS 库建表 SQL 必须同步 `COMMENT ON TABLE/COLUMN` 中文注释。PostGIS 等扩展自带系统表除外。

## 跨模块协作
- 后端接口、前端调用、SQL 初始化数据存在依赖时必须一致改动，按“接口契约 → 后端实现 → 前端调用 → SQL/文档”顺序梳理影响范围，避免只改其中一层。
- 生成代码、协议产物、依赖注入、自动导入文件统一用各模块既有命令生成，不手写伪生成结果。
- 生成命令执行成功后，其产物（`api/gen`、`pkg/gen`、`rpc` 等目录下文件）无需人工逐行验证正确性，以生成命令是否报错为准，避免重复读取全量生成文件。
- 涉及认证、上传、静态资源挂载或 admin 与 uni-app 共用能力时，先确认是否影响两个前端入口。

## 文档
- 调整顶层目录结构、启动方式、公共命令或模块关系时，必须同步更新根 `README.md`。
- 仓库级规则与文档保持轻量，只描述跨模块通用约束，不重复抄写子模块细则。
- 日志文件 *.log 必须使用中文注释。必须存放到 .log/{模块名}/ 目录下。
