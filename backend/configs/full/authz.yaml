# 授权配置模板。
# 项目使用 Casbin 执行接口权限校验；具体策略通常来自数据库或策略文件。
authz:
  casbin:
    # 授权策略前缀，用于区分不同应用或策略命名空间；为空表示使用默认前缀。
    prefix: ''
    # 排除授权检查的路由列表。每项同时按 URL 和 HTTP 方法匹配。
    exclude:
      - url: ''
        # 请求方法，例如 GET、POST；为空表示不限定方法。
        method: ''
