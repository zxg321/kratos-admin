registry:
  type: consul
  consul:
    address: "127.0.0.1:8500"
    scheme: "http"
    # 本地 Consul ACL Token，仅用于开发机验证，不提交到仓库。
    token: 03e92c67-5da7-3f4f-6659-8fb2d137aaa5
    # Admin 包含 scheduler 等非 TCP Endpoint，由注册器 TTL 心跳统一维护服务状态。
    health_check: false
